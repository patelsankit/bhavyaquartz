/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: 'export',
    images: {
        domains: ['www.evetis.in'], // Add the external domain here
      },
};

export default nextConfig;
    